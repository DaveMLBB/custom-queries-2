spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fhard
    username: root
    password: ciao
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      fail-on-empty-beans: false
